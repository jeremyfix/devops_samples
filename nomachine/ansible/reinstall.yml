- hosts:
    - slurm_master
  become: true
  roles:
    # - postfix
    # - ldap.client
    # - ntp.master
    - slurm.common
    - slurm
    - slurm.master
    - slurm.user

- hosts:
    - compute_nodes    
  become: true
  roles:
      - slurm.common
      - slurm
      - slurm.compute
      - nomachine
